<%= form_for @user do |form| %>
	<%= render :partial => "/users/partials/form", object: form %>

	<div class="submit-field form-group">
     <%= form.submit "Update", class: "btn btn-success" %>
   </div>
<% end %>
